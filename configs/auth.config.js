const SECRET_KEY = process.env.SECRET_KEY || "ADITYA123";                                                                    

// this secret key can never share with any one 

module.exports = SECRET_KEY;